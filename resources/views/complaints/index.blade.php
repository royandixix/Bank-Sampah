@extends('layouts.app') @section('title','Pengaduan') @section('content')@if(auth()->user()->role==='nasabah')<div class="text-end mb-3"><a class="btn btn-success" href="{{ route('complaints.create') }}">+ Buat Pengaduan</a></div>@endif<div class="row g-3">@forelse($complaints as $c)<div class="col-md-6"><div class="card h-100"><div class="card-body"><h5>{{ $c->subject }}</h5><small>{{ $c->customer->name }} · {{ $c->created_at->format('d/m/Y H:i') }}</small><p class="mt-2">{{ $c->message }}</p><span class="badge bg-secondary">{{ $c->status }}</span>@if($c->response)<div class="alert alert-light mt-2"><b>Tanggapan:</b> {{ $c->response }}</div>@endif @if(in_array(auth()->user()->role,['admin','petugas']))<hr><form method="post" action="{{ route('complaints.update',$c) }}">@csrf @method('patch')<select class="form-select mb-2" name="status"><option>processed</option><option>resolved</option><option>closed</option></select><textarea class="form-control mb-2" name="response" placeholder="Tanggapan"></textarea><button class="btn btn-sm btn-success">Update</button></form>@endif</div></div></div>@empty<div class="text-muted">Belum ada pengaduan.</div>@endforelse</div><div class="mt-3">{{ $complaints->links() }}</div>@endsection