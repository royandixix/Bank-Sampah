@extends('layouts.app') @section('title',$user->exists?'Edit Pengguna':'Tambah Pengguna') @section('content')<div class="card"><div class="card-body"><form method="post" action="{{ $user->exists?route('users.update',$user):route('users.store') }}">@csrf @if($user->exists)@method('put')@endif<div class="row g-3"><div class="col-md-6"><label>Nama</label><input class="form-control" name="name" value="{{ old('name',$user->name) }}" required></div><div class="col-md-6"><label>Email</label><input class="form-control" type="email" name="email" value="{{ old('email',$user->email) }}" required></div><div class="col-md-6"><label>Password {{ $user->exists?'(kosongkan jika tidak diganti)':'' }}</label><input class="form-control" type="password" name="password" @if(!$user->exists)required @endif></div><div class="col-md-6"><label>Role</label><select class="form-select" name="role">@foreach(['admin','petugas','nasabah'] as $x)<option @selected(old('role',$user->role)===$x)>{{ $x }}</option>@endforeach</select></div><div class="col-md-6"><label>No. HP</label><input class="form-control" name="phone" value="{{ old('phone',$user->phone) }}"></div><div class="col-md-6"><label>Alamat</label><input class="form-control" name="address" value="{{ old('address',$user->address) }}"></div>@if($user->exists)<div class="col-12"><label><input type="checkbox" name="is_active" value="1" @checked($user->is_active)> Aktif</label></div>@endif</div><button class="btn btn-success mt-3">Simpan</button></form></div></div>@endsection