@extends('layouts.app') @section('title','Pengguna') @section('content')<div class="d-flex gap-2 mb-3"><form class="d-flex gap-2 flex-grow-1"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari nama/email"><select class="form-select" name="role"><option value="">Semua role</option>@foreach(['admin','petugas','nasabah'] as $x)<option @selected(request('role')===$x)>{{ $x }}</option>@endforeach</select><button class="btn btn-outline-secondary">Filter</button></form><a class="btn btn-success" href="{{ route('users.create') }}">+ Pengguna</a></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Nama</th><th>Email</th><th>Role</th><th>Aktif</th><th>Aksi</th></tr></thead><tbody>@foreach($users as $u)<tr><td>{{ $u->name }}</td><td>{{ $u->email }}</td><td>{{ $u->role }}</td><td>{{ $u->is_active?'Ya':'Tidak' }}</td><td><a class="btn btn-sm btn-warning" href="{{ route('users.edit',$u) }}">Edit</a>@if($u->id!==auth()->id())<form class="d-inline" method="post" action="{{ route('users.destroy',$u) }}">@csrf @method('delete')<button class="btn btn-sm btn-danger">Hapus</button></form>@endif</td></tr>@endforeach</tbody></table></div></div><div class="mt-3">{{ $users->links() }}</div>@endsection