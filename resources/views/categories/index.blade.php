@extends('layouts.app') @section('title','Kategori Sampah') @section('content')<div class="d-flex gap-2 mb-3"><form class="d-flex gap-2"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari kategori"><button class="btn btn-outline-secondary">Cari</button></form><a class="btn btn-success ms-auto" href="{{ route('categories.create') }}">+ Tambah</a></div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Nama</th><th>Deskripsi</th><th width="180">Aksi</th></tr></thead><tbody>@forelse($cats as $c)<tr><td>{{ $c->name }}</td><td>{{ $c->description }}</td><td><a class="btn btn-sm btn-warning" href="{{ route('categories.edit',$c) }}">Edit</a><form class="d-inline" method="post" action="{{ route('categories.destroy',$c) }}">@csrf @method('delete')<button class="btn btn-sm btn-danger" onclick="return confirm('Hapus?')">Hapus</button></form></td></tr>@empty<tr><td colspan="3" class="text-center">Data kosong</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $cats->links() }}</div>@endsection